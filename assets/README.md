# 게임 에셋

게임에서 실제로 불러오는 리소스만 이 폴더에 둡니다.

- `images/backgrounds/`: 배경 이미지
- `images/characters/`: 캐릭터 이미지
- `images/props/`: 발판과 소품 이미지
- `images/ui/`: 게임 UI 이미지
- `audio/bgm/`: 배경음악
- `audio/sfx/`: 효과음
- `fonts/`: 로컬 글꼴

JavaScript와 CSS는 각각 루트의 `js/`, `css/`에 두고 Phaser 라이브러리는
`vendor/`에서 관리합니다.
